function calcula() {
    document.datos.respuesta1.value = Math.pow(parseFloat(document.datos.entrada.value), 2)
    document.datos.respuesta2.value = Math.pow(parseFloat(document.datos.entrada.value), 3)
}